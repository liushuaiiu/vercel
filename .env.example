# 代理访问令牌
# 这是一个额外的安全层，防止未授权访问代理服务
# 生成建议：使用强随机字符串（至少 32 位）
#
# 生成方法示例（在终端运行）：
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 或
# openssl rand -hex 32

PROXY_TOKEN=your-secret-proxy-token-here
